import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DS Experiences — Premium Live Event Ticketing",
  description: "The world's most exclusive live event experiences — UEFA Champions League, Formula 1, concerts, and hospitality packages across 38 countries.",
  keywords: ["tickets", "sports", "concerts", "formula 1", "hospitality", "UEFA", "premium ticketing", "DS Experiences"],
  authors: [{ name: "DS Experiences" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
